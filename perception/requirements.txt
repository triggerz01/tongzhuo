# 同桌 · 感知层依赖
# 注意：mediapipe / ultralytics 目前不支持 Python 3.14，请用 Python 3.12。

opencv-python>=4.9.0
# 必须锁在 1.0 以下：mediapipe 1.0 移除了 mp.solutions，
# 而新的 Tasks API 需要额外下载 .task 模型文件（Google CDN，国内大概率不通）。
# 0.10.x 把模型打包在 wheel 里，完全离线可用。
mediapipe>=0.10.14,<1.0
numpy>=1.26.0
websockets>=12.0

# 可选：手机检测。会拉 torch，体积较大（~2GB）。
# 不装也能跑，只是 phone 一类不生效。
ultralytics>=8.2.0
