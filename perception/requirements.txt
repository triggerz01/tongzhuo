# 同桌 · 感知层依赖
# 注意：mediapipe / ultralytics 目前不支持 Python 3.14，请用 Python 3.12。

opencv-python>=4.9.0
# 用的是 Tasks API（mediapipe.tasks.python.vision.FaceLandmarker）。
# 旧的 mp.solutions 已经从 mediapipe 里删掉了，0.10.35 和 1.x 都没有，别再找它。
# Tasks API 需要的模型文件已放在 perception/models/，随仓库分发，不用联网下。
mediapipe>=0.10.14
numpy>=1.26.0
websockets>=12.0

# 不需要 ultralytics / torch。
# 手机与人体检测用的是 mediapipe 自带的 ObjectDetector +
# EfficientDet-Lite0（13MB，已放在 models/ 随仓库分发）。
# 原本打算用 YOLOv8n，但它依赖 torch —— 124MB 的 wheel 在国内网络下
# 反复下不完（阿里、腾讯两个镜像都中途断）。换成 mediapipe 自带的之后
# 零新依赖，模型还小了一个数量级。
